---
name: 🔍 CI

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

on: # yamllint disable-line rule:truthy
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:
jobs:
  gitleaks:
    name: 🔑 Secret Scan
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: 🔑 Run Gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  lint:
    name: 🔍 Lint
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7

      - name: 🐍 Setup Python
        uses: actions/setup-python@v6
        with:
          python-version-file: .python-version

      - name: ⚡ Setup uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          activate-environment: true

      - name: 📦 Install dependencies
        run: uv sync --locked --group dev

      - name: 🔍 Run yamllint
        run: uv run yamllint .

      - name: 📦 Install Ansible Galaxy collections and roles
        run: |
          uv run ansible-galaxy collection install -r ansible/requirements.yml
          uv run ansible-galaxy role install -r ansible/requirements.yml -p ~/.local/share/ansible/roles

      - name: 🔍 Run ansible-lint
        run: cd ansible && uv run ansible-lint .

  helm-lint:
    name: ⎈ Helm Lint
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7

      - name: ⎈ Setup Helm
        uses: azure/setup-helm@v5

      - name: 🔍 Run Helm lint
        run: |
          find kubernetes -name "Chart.yaml" | xargs -I{} dirname {} | while read chart; do
            echo "Linting $chart"
            helm lint "$chart"
          done

  terraform-fmt:
    name: 🏗️ Terraform Format
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7

      - name: 🏗️ Setup Terraform
        uses: hashicorp/setup-terraform@v4

      - name: 🔍 Run Terraform format
        run: terraform fmt -check -recursive terraform/
